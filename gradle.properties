org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Pin JDK to Android Studio's bundled JBR (JDK 21).
# Required because the system default JDK 25 is too new for Kotlin 2.1.0's
# JavaVersion.parse() — it throws IllegalArgumentException: 25.0.2.
# If Android Studio is installed elsewhere, update this path.

